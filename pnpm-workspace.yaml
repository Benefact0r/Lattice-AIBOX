packages:
  - packages/*
  # apps/web lives in its own repo: github.com/Benefact0r/Lattice-AIBOX-WEB

# Native build scripts that pnpm asks about — these are optional native
# acceleration addons; safe to skip in our codebase.
ignoredBuiltDependencies:
  - bufferutil
  - utf-8-validate
  - bigint-buffer
